[project]
name = "claude-hooks"
version = "0.1.0"
description = "Claude Code hooks"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub>=1.8.0",
    "psutil>=5.9",
    "croniter>=2.0",
]
